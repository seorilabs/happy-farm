name: Deploy App Store

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: '업로드할 릴리즈 태그 (예: v1.0.0). 비우면 선택한 ref를 사용합니다.'
        required: false
        type: string
      upload_to_app_store:
        description: 'archive 생성 후 App Store Connect에 업로드'
        required: false
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: happy-farm-app-store-${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  archive-upload:
    name: iOS archive and upload
    runs-on: macos-15
    timeout-minutes: 60
    environment: app-store

    env:
      APP_STORE_RELEASE_REF: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
      SHOULD_UPLOAD_TO_APP_STORE: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.upload_to_app_store) }}
      IOS_SCHEME: HappyFarmMobile
      IOS_WORKSPACE: apps/mobile/ios/HappyFarmMobile.xcworkspace
      IOS_BUNDLE_ID: com.seorilabs.happyfarm
      IOS_PROVISIONING_PROFILE_NAME: AppStore Happy Farm Profile
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID || 'HCDUXX4Z3X' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}

      - name: Check release ref
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "$APP_STORE_RELEASE_REF" ]; then
            echo "release_tag input or release tag ref is required." >&2
            exit 1
          fi

          if ! [[ "$APP_STORE_RELEASE_REF" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "App Store deploy requires a vX.Y.Z release tag, got: $APP_STORE_RELEASE_REF" >&2
            exit 1
          fi

      - name: Setup pnpm workspace
        uses: ./.github/actions/setup-pnpm-workspace

      - name: Setup Ruby and CocoaPods
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          working-directory: apps/mobile

      - name: Resolve release version
        id: release-version
        env:
          RELEASE_TAG: ${{ env.APP_STORE_RELEASE_REF }}
        run: node scripts/resolve-release-version.mjs --github-output --github-step-summary --write-release-info --quiet

      - name: Typecheck mobile app
        run: pnpm --dir apps/mobile typecheck

      - name: Test mobile app
        run: pnpm --dir apps/mobile test --watchAll=false

      - name: Restore Firebase iOS config
        shell: bash
        env:
          FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64: ${{ secrets.FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64 }}
        run: |
          set -euo pipefail

          if [ -z "$FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64" ]; then
            echo "FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64 secret is required for App Store builds." >&2
            exit 1
          fi

          output_path="apps/mobile/ios/HappyFarmMobile/GoogleService-Info.plist"
          if ! printf '%s' "$FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64" | base64 --decode > "$output_path" 2>/dev/null; then
            printf '%s' "$FIREBASE_IOS_GOOGLE_SERVICE_INFO_PLIST_BASE64" | base64 -D > "$output_path"
          fi

          plutil -lint "$output_path"

      - name: Validate signing secrets
        shell: bash
        env:
          APPLE_DISTRIBUTION_CERTIFICATE_BASE64: ${{ secrets.APPLE_DISTRIBUTION_CERTIFICATE_BASE64 }}
          APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD }}
          APPLE_PROVISIONING_PROFILE_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE_BASE64 }}
          APPLE_KEYCHAIN_PASSWORD: ${{ secrets.APPLE_KEYCHAIN_PASSWORD }}
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_PRIVATE_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY_BASE64 }}
        run: |
          set -euo pipefail

          required_signing_vars=(
            APPLE_DISTRIBUTION_CERTIFICATE_BASE64
            APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD
            APPLE_PROVISIONING_PROFILE_BASE64
            APPLE_KEYCHAIN_PASSWORD
            APPLE_TEAM_ID
          )

          for var_name in "${required_signing_vars[@]}"; do
            if [ -z "${!var_name:-}" ]; then
              echo "$var_name is required." >&2
              exit 1
            fi
          done

          if [ "$SHOULD_UPLOAD_TO_APP_STORE" = "true" ]; then
            required_upload_vars=(
              APP_STORE_CONNECT_API_KEY_ID
              APP_STORE_CONNECT_ISSUER_ID
              APP_STORE_CONNECT_PRIVATE_KEY_BASE64
            )

            for var_name in "${required_upload_vars[@]}"; do
              if [ -z "${!var_name:-}" ]; then
                echo "$var_name is required when upload_to_app_store is true." >&2
                exit 1
              fi
            done
          fi

      - name: Restore Apple signing assets
        id: signing
        shell: bash
        env:
          APPLE_DISTRIBUTION_CERTIFICATE_BASE64: ${{ secrets.APPLE_DISTRIBUTION_CERTIFICATE_BASE64 }}
          APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD }}
          APPLE_PROVISIONING_PROFILE_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE_BASE64 }}
          APPLE_KEYCHAIN_PASSWORD: ${{ secrets.APPLE_KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail

          decode_base64() {
            local encoded="$1"
            local output="$2"
            if ! printf '%s' "$encoded" | base64 --decode > "$output" 2>/dev/null; then
              printf '%s' "$encoded" | base64 -D > "$output"
            fi
          }

          signing_dir="$RUNNER_TEMP/happy-farm-app-store-signing"
          keychain_path="$signing_dir/app-store-signing.keychain-db"
          certificate_path="$signing_dir/apple-distribution.p12"
          profile_path="$signing_dir/app-store.mobileprovision"

          mkdir -p "$signing_dir"
          decode_base64 "$APPLE_DISTRIBUTION_CERTIFICATE_BASE64" "$certificate_path"
          decode_base64 "$APPLE_PROVISIONING_PROFILE_BASE64" "$profile_path"

          security create-keychain -p "$APPLE_KEYCHAIN_PASSWORD" "$keychain_path"
          security set-keychain-settings -lut 21600 "$keychain_path"
          security unlock-keychain -p "$APPLE_KEYCHAIN_PASSWORD" "$keychain_path"
          security import "$certificate_path" -P "$APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$keychain_path"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$APPLE_KEYCHAIN_PASSWORD" "$keychain_path"
          security default-keychain -s "$keychain_path"
          security list-keychains -d user -s "$keychain_path"

          profile_plist="$signing_dir/app-store-profile.plist"
          security cms -D -i "$profile_path" > "$profile_plist"

          profile_uuid="$(plutil -extract UUID raw -o - "$profile_plist")"
          profile_name="$(plutil -extract Name raw -o - "$profile_plist")"
          profile_team_identifier="$(plutil -extract TeamIdentifier.0 raw -o - "$profile_plist")"
          profile_app_identifier="$(plutil -extract Entitlements.application-identifier raw -o - "$profile_plist")"
          profile_get_task_allow="$(plutil -extract Entitlements.get-task-allow raw -o - "$profile_plist")"
          profile_provisions_all_devices="$(plutil -extract ProvisionsAllDevices raw -o - "$profile_plist" 2>/dev/null || true)"
          profile_has_devices="false"
          if /usr/libexec/PlistBuddy -c 'Print :ProvisionedDevices' "$profile_plist" >/dev/null 2>&1; then
            profile_has_devices="true"
          fi

          profile_cert_fingerprints=""
          profile_cert_index=0
          while true; do
            profile_cert_path="$signing_dir/profile-cert-$profile_cert_index.cer"
            if ! plutil -extract "DeveloperCertificates.$profile_cert_index" raw -o "$profile_cert_path" "$profile_plist" 2>/dev/null; then
              break
            fi

            if [ -s "$profile_cert_path" ]; then
              profile_cert_fingerprint="$(openssl x509 -inform DER -in "$profile_cert_path" -noout -fingerprint -sha1 2>/dev/null | sed 's/^sha1 Fingerprint=//;s/://g' | tr '[:lower:]' '[:upper:]')"
              if [ -n "$profile_cert_fingerprint" ]; then
                profile_cert_fingerprints="$profile_cert_fingerprints $profile_cert_fingerprint"
              fi
            fi

            profile_cert_index=$((profile_cert_index + 1))
          done
          profile_cert_fingerprints="$(echo "$profile_cert_fingerprints" | tr ' ' '\n' | awk 'NF' | sort -u | tr '\n' ' ' | sed 's/[[:space:]]*$//')"
          imported_cert_fingerprints="$(security find-certificate -a -Z -c "Apple Distribution" "$keychain_path" | awk '/SHA-1 hash:/{print $3}' | sort -u | tr '\n' ' ' | sed 's/[[:space:]]*$//')"
          profile_cert_match="false"
          for imported_cert_fingerprint in $imported_cert_fingerprints; do
            for profile_cert_fingerprint in $profile_cert_fingerprints; do
              if [ "$imported_cert_fingerprint" = "$profile_cert_fingerprint" ]; then
                profile_cert_match="true"
              fi
            done
          done

          profile_type="app-store"
          if [ "$profile_get_task_allow" = "true" ]; then
            profile_type="development"
          elif [ "$profile_has_devices" = "true" ]; then
            profile_type="ad-hoc"
          elif [ "$profile_provisions_all_devices" = "true" ]; then
            profile_type="enterprise"
          fi

          expected_app_identifier="$APPLE_TEAM_ID.$IOS_BUNDLE_ID"
          wildcard_app_identifier="$APPLE_TEAM_ID.*"
          profile_bundle_match="false"
          if [ "$profile_app_identifier" = "$expected_app_identifier" ] || [ "$profile_app_identifier" = "$wildcard_app_identifier" ]; then
            profile_bundle_match="true"
          fi

          profile_team_match="false"
          if [ "$profile_team_identifier" = "$APPLE_TEAM_ID" ]; then
            profile_team_match="true"
          fi

          echo "Provisioning profile diagnostics:"
          echo "  name: $profile_name"
          echo "  uuid: $profile_uuid"
          echo "  inferred type: $profile_type"
          echo "  team matches APPLE_TEAM_ID: $profile_team_match"
          echo "  bundle matches IOS_BUNDLE_ID: $profile_bundle_match"
          echo "  get-task-allow: $profile_get_task_allow"
          echo "  has provisioned devices: $profile_has_devices"
          echo "  provisions all devices: ${profile_provisions_all_devices:-false}"
          echo "  profile contains imported Apple Distribution certificate: $profile_cert_match"
          echo "Imported signing identities:"
          security find-identity -v -p codesigning "$keychain_path"

          {
            echo "### App Store signing profile"
            echo ""
            echo "- name: $profile_name"
            echo "- uuid: $profile_uuid"
            echo "- inferred type: $profile_type"
            echo "- team matches APPLE_TEAM_ID: $profile_team_match"
            echo "- bundle matches IOS_BUNDLE_ID: $profile_bundle_match"
            echo "- get-task-allow: $profile_get_task_allow"
            echo "- has provisioned devices: $profile_has_devices"
            echo "- provisions all devices: ${profile_provisions_all_devices:-false}"
            echo "- profile contains imported Apple Distribution certificate: $profile_cert_match"
          } >> "$GITHUB_STEP_SUMMARY"

          if [ "$profile_team_match" != "true" ]; then
            echo "Provisioning profile team does not match APPLE_TEAM_ID." >&2
            exit 1
          fi

          if [ "$profile_bundle_match" != "true" ]; then
            echo "Provisioning profile bundle does not match IOS_BUNDLE_ID=$IOS_BUNDLE_ID." >&2
            exit 1
          fi

          if [ "$profile_get_task_allow" != "false" ]; then
            echo "App Store provisioning profile must have get-task-allow=false." >&2
            exit 1
          fi

          if [ "$profile_has_devices" = "true" ] || [ "$profile_provisions_all_devices" = "true" ]; then
            echo "App Store provisioning profile must not include devices or provision all devices." >&2
            exit 1
          fi

          if [ "$profile_cert_match" != "true" ]; then
            echo "Provisioning profile does not include the imported Apple Distribution certificate." >&2
            exit 1
          fi

          profiles_dir="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$profiles_dir"
          cp "$profile_path" "$profiles_dir/$profile_uuid.mobileprovision"

          echo "keychain_path=$keychain_path" >> "$GITHUB_OUTPUT"
          echo "profile_uuid=$profile_uuid" >> "$GITHUB_OUTPUT"
          echo "profile_name=$profile_name" >> "$GITHUB_OUTPUT"

      - name: Install CocoaPods
        working-directory: apps/mobile/ios
        env:
          BUNDLE_GEMFILE: ../Gemfile
        run: bundle exec pod install

      - name: Validate App Store readiness
        continue-on-error: true
        run: pnpm check:app-store -- --json

      - name: Show resolved iOS signing settings
        shell: bash
        env:
          APPLE_MARKETING_VERSION: ${{ steps.release-version.outputs.apple_marketing_version }}
          APPLE_BUILD_NUMBER: ${{ steps.release-version.outputs.apple_build_number }}
          KEYCHAIN_PATH: ${{ steps.signing.outputs.keychain_path }}
          RESOLVED_IOS_PROFILE_NAME: ${{ steps.signing.outputs.profile_name }}
        run: |
          set -euo pipefail

          xcodebuild -workspace "$IOS_WORKSPACE" \
            -scheme "$IOS_SCHEME" \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -showBuildSettings \
            MARKETING_VERSION="$APPLE_MARKETING_VERSION" \
            CURRENT_PROJECT_VERSION="$APPLE_BUILD_NUMBER" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            IOS_PROVISIONING_PROFILE_NAME="$RESOLVED_IOS_PROFILE_NAME" \
            OTHER_CODE_SIGN_FLAGS="--keychain $KEYCHAIN_PATH" \
            | awk '
              /Build settings for action build and target HappyFarmMobile:/ { in_app_target = 1; print; next }
              /Build settings for action build and target / { in_app_target = 0 }
              in_app_target && /CODE_SIGN|DEVELOPMENT_TEAM|PRODUCT_BUNDLE_IDENTIFIER|PROVISIONING_PROFILE|IOS_PROVISIONING_PROFILE_NAME|SDKROOT/ { print }
            '

      - name: Archive iOS app
        id: archive
        shell: bash
        env:
          APPLE_MARKETING_VERSION: ${{ steps.release-version.outputs.apple_marketing_version }}
          APPLE_BUILD_NUMBER: ${{ steps.release-version.outputs.apple_build_number }}
          KEYCHAIN_PATH: ${{ steps.signing.outputs.keychain_path }}
          RESOLVED_IOS_PROFILE_NAME: ${{ steps.signing.outputs.profile_name }}
        run: |
          set -euo pipefail

          archive_path="$RUNNER_TEMP/HappyFarmMobile-v${APPLE_MARKETING_VERSION}-${APPLE_BUILD_NUMBER}.xcarchive"

          xcodebuild archive \
            -workspace "$IOS_WORKSPACE" \
            -scheme "$IOS_SCHEME" \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$archive_path" \
            MARKETING_VERSION="$APPLE_MARKETING_VERSION" \
            CURRENT_PROJECT_VERSION="$APPLE_BUILD_NUMBER" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            IOS_PROVISIONING_PROFILE_NAME="$RESOLVED_IOS_PROFILE_NAME" \
            OTHER_CODE_SIGN_FLAGS="--keychain $KEYCHAIN_PATH"

          app_info_plist="$archive_path/Products/Applications/HappyFarmMobile.app/Info.plist"
          actual_marketing_version="$(/usr/libexec/PlistBuddy -c 'Print CFBundleShortVersionString' "$app_info_plist")"
          actual_build_number="$(/usr/libexec/PlistBuddy -c 'Print CFBundleVersion' "$app_info_plist")"

          if [ "$actual_marketing_version" != "$APPLE_MARKETING_VERSION" ]; then
            echo "Archive marketing version mismatch: $actual_marketing_version != $APPLE_MARKETING_VERSION" >&2
            exit 1
          fi

          if [ "$actual_build_number" != "$APPLE_BUILD_NUMBER" ]; then
            echo "Archive build number mismatch: $actual_build_number != $APPLE_BUILD_NUMBER" >&2
            exit 1
          fi

          {
            echo "archive_path=$archive_path"
            echo "marketing_version=$actual_marketing_version"
            echo "build_number=$actual_build_number"
          } >> "$GITHUB_OUTPUT"

          {
            echo "### App Store archive"
            echo ""
            echo "- archive: $archive_path"
            echo "- CFBundleShortVersionString: $actual_marketing_version"
            echo "- CFBundleVersion: $actual_build_number"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload xcarchive artifact
        uses: actions/upload-artifact@v7
        with:
          name: happy-farm-app-store-archive-v${{ steps.archive.outputs.marketing_version }}-build${{ steps.archive.outputs.build_number }}-${{ github.sha }}
          if-no-files-found: error
          retention-days: 7
          path: ${{ steps.archive.outputs.archive_path }}

      - name: Prepare App Store Connect API key
        if: env.SHOULD_UPLOAD_TO_APP_STORE == 'true'
        id: app-store-api-key
        shell: bash
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_PRIVATE_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY_BASE64 }}
        run: |
          set -euo pipefail

          api_key_dir="$RUNNER_TEMP/app-store-connect-api-key"
          api_key_path="$api_key_dir/AuthKey_${APP_STORE_CONNECT_API_KEY_ID}.p8"
          mkdir -p "$api_key_dir"

          if ! printf '%s' "$APP_STORE_CONNECT_PRIVATE_KEY_BASE64" | base64 --decode > "$api_key_path" 2>/dev/null; then
            printf '%s' "$APP_STORE_CONNECT_PRIVATE_KEY_BASE64" | base64 -D > "$api_key_path"
          fi

          chmod 600 "$api_key_path"
          echo "api_key_path=$api_key_path" >> "$GITHUB_OUTPUT"

      - name: Upload archive to App Store Connect
        if: env.SHOULD_UPLOAD_TO_APP_STORE == 'true'
        shell: bash
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_PATH: ${{ steps.app-store-api-key.outputs.api_key_path }}
          RESOLVED_IOS_PROFILE_NAME: ${{ steps.signing.outputs.profile_name }}
        run: |
          set -euo pipefail

          export_options_plist="$RUNNER_TEMP/AppStoreExportOptions.plist"
          export_path="$RUNNER_TEMP/app-store-export"

          cat > "$export_options_plist" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store-connect</string>
            <key>destination</key>
            <string>upload</string>
            <key>manageAppVersionAndBuildNumber</key>
            <false/>
            <key>signingStyle</key>
            <string>manual</string>
            <key>teamID</key>
            <string>${APPLE_TEAM_ID}</string>
            <key>signingCertificate</key>
            <string>Apple Distribution</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${IOS_BUNDLE_ID}</key>
              <string>${RESOLVED_IOS_PROFILE_NAME}</string>
            </dict>
            <key>stripSwiftSymbols</key>
            <true/>
            <key>uploadSymbols</key>
            <true/>
          </dict>
          </plist>
          EOF

          plutil -lint "$export_options_plist"

          xcodebuild -exportArchive \
            -archivePath "${{ steps.archive.outputs.archive_path }}" \
            -exportOptionsPlist "$export_options_plist" \
            -exportPath "$export_path" \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$APP_STORE_CONNECT_API_KEY_PATH" \
            -authenticationKeyID "$APP_STORE_CONNECT_API_KEY_ID" \
            -authenticationKeyIssuerID "$APP_STORE_CONNECT_ISSUER_ID"
