# WorkflowBundle v5 generator가 관리합니다. 수동 편집하지 마십시오.
name: Org Contract
on:
  pull_request:
    paths:
      - .github/workflows/org-contract.yml
permissions:
  contents: read
  id-token: write
  packages: read
concurrency:
  group: org-contract-${{ github.repository_id }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  org-contract:
    uses: seorilabs/.github/.github/workflows/js-static-checks-v1.yml@7790257716bca71ae865aa11274803520adf13e3
