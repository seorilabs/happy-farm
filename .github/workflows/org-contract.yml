# WorkflowBundle candidate canary generator가 관리합니다. 수동 편집하지 마십시오.
name: Org Contract

on:
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read
  packages: read

concurrency:
  group: org-contract-${{ github.repository_id }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  org-contract:
    name: Org Contract
    uses: seorilabs/.github/.github/workflows/rn-static-checks-v2.yml@02d748c646a848f99889cda00e7a5812dfe880fb
    with:
      package_manager: pnpm
      working_directory: .
