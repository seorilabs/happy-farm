# WorkflowBundle v5 generator가 관리합니다. 수동 편집하지 마십시오.
name: Android Build-only
on:
  pull_request:
    paths:
      - .github/workflows/android-build-only.yml
permissions:
  contents: read
  id-token: write
  packages: read
concurrency:
  group: android-build-${{ github.repository_id }}-${{ github.ref }}
  cancel-in-progress: false
jobs:
  android-build:
    uses: seorilabs/.github/.github/workflows/rn-build-android-cloud-v2.yml@2e9b6afd0c0b02f691ecaf1fa0055cadb57e972c
