node_modules/
dist/
npm-debug.log

.yarn/*
.pnp.*
yarn-error.log
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.env
.firebase/
.granite
.swc
.DS_Store
*.ait

# Google Play / Android release secrets
*.jks
*.keystore
android/key.properties
**/android/key.properties
play-store/secrets/
play-store/service-account*.json
gha-creds-*.json

# Apple App Store / iOS release secrets
*.p12
*.mobileprovision
AuthKey_*.p8
app-store/secrets/

# Android Firebase client config is restored from CI secrets (Google Play builds).
google-services.json
# iOS GoogleService-Info.plist 은 저장소(private)에 커밋한다 — 클라이언트 config 라
# 앱 바이너리에 어차피 포함되고, Xcode Cloud/로컬 빌드가 시크릿 설정 없이 쓰게 한다.

# game-asset-pipeline raw generations & QA sheet (regenerate via assets/art/asset-manifest.json)
assets/art/raw/
assets/art/qa_sheet.png

# game-sound-pipeline raw generations (regenerate via assets/audio/sound-manifest.json)
assets/audio/raw/

# Python 캐시(스토어 업로드 스크립트 실행 부산물)
__pycache__/
*.pyc
